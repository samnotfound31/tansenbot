# Discord library (app_commands used -> discord.py 2.x or compatible fork)
discord.py>=2.0.0

# HTTP / async http
aiohttp>=3.8.1
requests>=2.28.0

# YT download/streaming
yt-dlp>=2023.12.1

# HTML parsing for Genius scraping
beautifulsoup4>=4.12.2

# For reading .env locally and env helper
python-dotenv>=1.0.0

# Spotify helper (optional, only if you use Spotipy OAuth flows)
spotipy>=2.22.1

# Database driver (only needed if you switch to Postgres). SQLite is builtin.
psycopg2-binary>=2.9.7

# Flask for keep-alive endpoint (keep_alive.py uses Flask if available)
Flask>=2.2.5

# Optional but recommended utilities
typing_extensions>=4.5.0

# NOTE: ffmpeg is a system dependency and NOT installed via pip.
# Make sure ffmpeg is available on the host (Railway/Nixpacks/Oracle/your machine).
PyNaCl==1.5.0
